### สรุปการเปลี่ยนแปลงวันที่ 8 พฤษภาคม 2025
การเปลี่ยนแปลงหลัก
1. เปลี่ยน Default Route เป็น OEE Dashboard

แก้ไข dashboard-routing.module.ts
เปลี่ยน redirect จาก 'nfts' เป็น 'oee'
2. ปรับโครงสร้างเมนูใน menu.ts

ย้าย Production Group ขึ้นมาเป็นอันดับแรก
แก้ไข route paths ให้ถูกต้อง
จัดกลุ่มเมนูใหม่
3. แก้ไข Component ที่เกี่ยวข้อง

ปรับ dashboard.component.ts เป็น standalone
เพิ่ม RouterOutlet และ styles
4. เพิ่ม HMR Support

ติดตั้ง @angularclass/hmr รายละเอียดการติดตั้งอ่านได้ที่ HMR.md
สร้าง hmr.ts
เปิดใช้งาน HMR ใน angular.json

# ไฟล์ที่มีการเปลี่ยนแปลง
1. dashboard-routing.module.ts
2. menu.ts
3. dashboard.component.ts
4. hmr.ts (ไฟล์ใหม่)
5. angular.json (เพิ่ม HMR config)
6. package.json (เพิ่ม @angularclass/hmr)

# grb