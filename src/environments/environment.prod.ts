export const environment = {
  production: true,
  apiUrl: 'https://your-production-api-url',  // เปลี่ยนเป็น URL จริงตอน deploy
  auth: {
    testUsers: {} // ไม่จำเป็นต้องมี test users ใน production
  }
};